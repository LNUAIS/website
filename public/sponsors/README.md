# Sponsor logos go here, referenced from src/content/sponsors/*.md as
# logo: /sponsors/<file>.svg
#
# Tiles are 274x104 and logos are contained, never cropped. The tile is a light
# chip by default; for a white/negative mark set `background: dark` on the
# sponsor so it sits on the page's own ground instead.
