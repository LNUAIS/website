# Board portraits go here, referenced from src/content/board/*.md as
# photo: /board/<file>.webp
#
# WebP, around 600px on the long edge. next/image resizes and re-encodes per
# request, so anything much larger is wasted bytes in the repo.
